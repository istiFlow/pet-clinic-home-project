spring:
  datasource:
    url: jdbc:mysql://localhost:3306/allatklinika
    username: pisti
    password: Passw0rd!*
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: create-drop

server:
  port: 8080

